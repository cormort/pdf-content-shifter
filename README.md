# PDF 原座標分頁調整器

單檔 HTML 工具：把文字型 PDF 以原始頁面影像切成水平「內容帶」，在不重新排字、不替換字型的前提下調整內容的跨頁位置，最後用瀏覽器列印／存成 PDF。

- 檔案只在本機瀏覽器處理，不上傳。
- 需要網路載入 PDF.js（CDN）。

## 用法

開啟 `index.html`，把 PDF 拖進去。每頁預設為 🔒 鎖定；要調整某頁，先按該頁標題的鎖切成 🔓，再點選內容帶並用「↓ 推到下頁 / ↑ 拉回上頁」搬移。推到最後一頁時會自動新增一頁。
